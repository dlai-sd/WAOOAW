# Google OAuth Configuration for Platform Portal
# Get these from https://console.cloud.google.com/apis/credentials

# Google OAuth Client ID (from Google Cloud Console)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Google OAuth Client Secret (for backend token verification)
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Note: Platform Portal uses Python/Reflex, so no VITE_ prefix needed.
# These are server-side environment variables.
#
# To get your Google OAuth credentials:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable Google+ API or Google Identity Services
# 4. Go to Credentials
# 5. Create OAuth 2.0 Client ID (Web application)
# 6. Add authorized JavaScript origins:
#    - http://localhost:3000 (development)
#    - https://*.app.github.dev (Codespaces)
#    - https://pp.waooaw.com (production Platform Portal)
# 7. Add authorized redirect URIs (not strictly needed for modal OAuth):
#    - http://localhost:3000/auth/callback
#    - https://*.app.github.dev/auth/callback
#    - https://pp.waooaw.com/auth/callback
# 8. Copy the Client ID and Client Secret here

# Environment (auto-detected, but can override)
# ENV=development  # development, codespace, demo, uat, production
