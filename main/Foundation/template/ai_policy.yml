# AI policy template
containment:
  actions:
    - enter_support_mode
    - restrict_to_support_only_in_trial
  modes:
    trial_support_only:
      description: "Support-only trial mode; prohibits all job-related work, including internal drafts."
      prohibits:
        - job_related_work
        - internal_drafts
l3_update_policy:
  triggers:
    - state_change
  cadence: "2h"
