name: Mobile Google Play Store Deployment

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        type: choice
        options:
          - demo
          - uat
          - prod
      track:
        description: 'Play Store track'
        required: true
        type: choice
        options:
          - internal
          - alpha
          - beta
          - production
      release_notes:
        description: 'Release notes (optional)'
        required: false
        type: string
        default: 'Bug fixes and performance improvements'
      build_method:
        description: 'Build method (expo = Expo cloud, local-eas = free local build, existing = use build_id below)'
        required: true
        type: choice
        options:
          - expo
          - local-eas
          - existing
        default: 'expo'
      build_id:
        description: 'Existing EAS build ID to submit (only used when build_method=existing)'
        required: false
        type: string
        default: ''

permissions:
  contents: write
  id-token: write

jobs:
  prepare:
    name: Prepare Deployment
    runs-on: ubuntu-latest
    outputs:
      environment: ${{ steps.config.outputs.environment }}
      version: ${{ steps.config.outputs.version }}
      track: ${{ steps.config.outputs.track }}
      build-profile: ${{ steps.config.outputs.build-profile }}
      is-production: ${{ steps.config.outputs.is-production }}

    steps:
      - name: Determine deployment configuration
        id: config
        run: |
          # Manual dispatch takes precedence
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "environment=${{ github.event.inputs.environment }}" >> $GITHUB_OUTPUT
            echo "track=${{ github.event.inputs.track }}" >> $GITHUB_OUTPUT
            echo "version=manual-${{ github.run_number }}" >> $GITHUB_OUTPUT
            # eas.json profiles match environment names exactly: demo, uat, prod
            echo "build-profile=${{ github.event.inputs.environment }}" >> $GITHUB_OUTPUT
            echo "is-production=${{ github.event.inputs.environment == 'prod' }}" >> $GITHUB_OUTPUT

          # Push to fix branch - use demo environment
          elif [[ "${{ github.event_name }}" == "push" ]]; then
            echo "environment=demo" >> $GITHUB_OUTPUT
            echo "track=internal" >> $GITHUB_OUTPUT
            echo "version=auto-${{ github.run_number }}" >> $GITHUB_OUTPUT
            echo "build-profile=demo" >> $GITHUB_OUTPUT
            echo "is-production=false" >> $GITHUB_OUTPUT

          # Tag-based deployment
          elif [[ "${{ github.ref }}" =~ mobile-playstore-v([0-9]+\.[0-9]+\.[0-9]+)-demo ]]; then
            echo "environment=demo" >> $GITHUB_OUTPUT
            echo "track=internal" >> $GITHUB_OUTPUT
            echo "version=${BASH_REMATCH[1]}" >> $GITHUB_OUTPUT
            echo "build-profile=demo" >> $GITHUB_OUTPUT
            echo "is-production=false" >> $GITHUB_OUTPUT

          elif [[ "${{ github.ref }}" =~ mobile-playstore-v([0-9]+\.[0-9]+\.[0-9]+)-uat ]]; then
            echo "environment=uat" >> $GITHUB_OUTPUT
            echo "track=alpha" >> $GITHUB_OUTPUT
            echo "version=${BASH_REMATCH[1]}" >> $GITHUB_OUTPUT
            echo "build-profile=uat" >> $GITHUB_OUTPUT
            echo "is-production=false" >> $GITHUB_OUTPUT

          elif [[ "${{ github.ref }}" =~ mobile-playstore-v([0-9]+\.[0-9]+\.[0-9]+)$ ]]; then
            echo "environment=prod" >> $GITHUB_OUTPUT
            echo "track=production" >> $GITHUB_OUTPUT
            echo "version=${BASH_REMATCH[1]}" >> $GITHUB_OUTPUT
            echo "build-profile=prod" >> $GITHUB_OUTPUT
            echo "is-production=true" >> $GITHUB_OUTPUT

          else
            echo "❌ Invalid trigger. Use workflow_dispatch or tag format: mobile-playstore-v1.2.3"
            exit 1
          fi

      - name: Display configuration
        run: |
          echo "🚀 Deployment Configuration:"
          echo "  Environment: ${{ steps.config.outputs.environment }}"
          echo "  Version: ${{ steps.config.outputs.version }}"
          echo "  Track: ${{ steps.config.outputs.track }}"
          echo "  Build Profile: ${{ steps.config.outputs.build-profile }}"
          echo "  Is Production: ${{ steps.config.outputs.is-production }}"

  lint-and-test:
    name: Quality Checks
    runs-on: ubuntu-latest
    needs: prepare

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: src/mobile/package-lock.json

      - name: Install dependencies
        working-directory: src/mobile
        run: npm ci

      - name: Preflight versioning guard
        working-directory: src/mobile
        run: |
          set -euo pipefail

          echo "🔎 Preflight: validating committed versioning configuration"
          echo "  Expected SHA: ${{ github.sha }}"
          echo "  Checked out SHA: $(git rev-parse HEAD)"
          echo "  Build profile: ${{ needs.prepare.outputs.build-profile }}"

          APP_VERSION_CODE=$(jq -r '.expo.android.versionCode // empty' app.json)
          APP_VERSION_SOURCE=$(jq -r '.cli.appVersionSource // "local"' eas.json)
          PROFILE_AUTO_INCREMENT=$(jq -r '.build["${{ needs.prepare.outputs.build-profile }}"].android.autoIncrement // empty' eas.json)

          if [[ -z "$APP_VERSION_CODE" || ! "$APP_VERSION_CODE" =~ ^[0-9]+$ ]]; then
            echo "❌ Invalid app.json android.versionCode: '$APP_VERSION_CODE'"
            exit 1
          fi

          if [[ "$APP_VERSION_SOURCE" != "remote" ]]; then
            echo "❌ eas.json cli.appVersionSource must be 'remote' but is '$APP_VERSION_SOURCE'"
            echo "   Fix by committing src/mobile/eas.json with appVersionSource=remote before rerun."
            exit 1
          fi

          if [[ "$PROFILE_AUTO_INCREMENT" != "versionCode" ]]; then
            echo "❌ eas.json build profile '${{ needs.prepare.outputs.build-profile }}' android.autoIncrement must be 'versionCode' but is '$PROFILE_AUTO_INCREMENT'"
            exit 1
          fi

          echo "✅ Preflight passed"
          echo "  app.json android.versionCode: $APP_VERSION_CODE"
          echo "  eas.json cli.appVersionSource: $APP_VERSION_SOURCE"
          echo "  profile android.autoIncrement: $PROFILE_AUTO_INCREMENT"

      - name: Validate EAS config
        working-directory: src/mobile
        run: |
          npx eas-cli@latest config --platform android --profile ${{ needs.prepare.outputs.build-profile }} --non-interactive >/tmp/eas-config.json
          echo "✅ EAS config is valid for Android build profile '${{ needs.prepare.outputs.build-profile }}'"
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      - name: Verify checked-out commit and plugin config
        working-directory: src/mobile
        run: |
          echo "🔎 Checked out SHA: ${{ github.sha }}"
          git rev-parse HEAD
          echo "🔎 expo-font plugin entries in app.json:"
          jq -r '.expo.plugins // []' app.json

      - name: Run linter
        working-directory: src/mobile
        env:
          ESLINT_USE_FLAT_CONFIG: false
        run: npm run lint

      - name: Run type check
        working-directory: src/mobile
        run: npm run typecheck

      - name: Run tests
        working-directory: src/mobile
        run: npm test -- --ci --maxWorkers=2

  build-and-submit:
    name: Build & Submit to Play Store
    runs-on: ubuntu-latest
    needs: [prepare, lint-and-test]
    environment:
      name: mobile-${{ needs.prepare.outputs.environment }}
      url: https://play.google.com/console

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: src/mobile/package-lock.json

      - name: Setup Expo and EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Install dependencies
        working-directory: src/mobile
        run: npm ci

      - name: Update app version
        working-directory: src/mobile
        run: |
          # Use the version already committed in app.json — do not overwrite with
          # "manual-{run_number}". The versionCode is managed by EAS remotely
          # (appVersionSource: remote + autoIncrement: versionCode), so the only
          # thing that matters here is the human-readable semantic version string.
          CURRENT_VERSION=$(jq -r '.expo.version' app.json)
          echo "📦 App version: $CURRENT_VERSION (versionCode auto-incremented by EAS)"

      - name: Setup Google Play service account
        working-directory: src/mobile
        run: |
          echo "🔑 Setting up Google Play service account..."
          mkdir -p secrets
          echo '${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT_JSON }}' > secrets/google-play-service-account.json
          chmod 600 secrets/google-play-service-account.json
          echo "✅ Service account configured"

      - name: Link EAS Project
        if: github.event.inputs.build_method != 'existing'
        working-directory: src/mobile
        run: |
          echo "🔗 Linking EAS project (required for non-interactive builds)..."
          eas init --id fdb3bbde-a0e0-43f9-bf55-e780ecc563e7 --non-interactive
          echo "✅ EAS project linked"
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      - name: Sync EAS Secrets from GitHub Secrets
        if: github.event.inputs.build_method != 'existing'
        working-directory: src/mobile
        run: |
          echo "🔐 Syncing build secrets to EAS project..."
          # Delete ALL variants (old _DEMO-suffixed names and current final names)
          # Some may exist as sensitive type from prior runs which blocks env:push
          for VAR in \
            EXPO_PUBLIC_API_URL EXPO_PUBLIC_API_URL_DEMO \
            EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID_DEMO \
            EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID_DEMO \
            EXPO_PUBLIC_OAUTH_REDIRECT_SCHEME EXPO_PUBLIC_OAUTH_REDIRECT_SCHEME_DEMO; do
            eas env:delete production --variable-name "$VAR" --non-interactive 2>/dev/null || true
          done
          # Push with FINAL names — eas.json env block no longer overrides these,
          # so EAS environment vars auto-inject during build
          {
            echo "EXPO_PUBLIC_API_URL=${{ secrets.EXPO_PUBLIC_API_URL_DEMO }}"
            echo "EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=${{ secrets.EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID_DEMO }}"
            echo "EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=${{ secrets.EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID_DEMO }}"
            echo "EXPO_PUBLIC_OAUTH_REDIRECT_SCHEME=${{ secrets.EXPO_PUBLIC_OAUTH_REDIRECT_SCHEME_DEMO }}"
          } > /tmp/eas-secrets.env
          eas env:push production --path /tmp/eas-secrets.env --force
          rm -f /tmp/eas-secrets.env
          echo "✅ EAS secrets synced"
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      - name: Use Existing Build
        if: github.event.inputs.build_method == 'existing'
        run: |
          BUILD_ID="${{ github.event.inputs.build_id }}"
          if [ -z "$BUILD_ID" ]; then
            echo "❌ build_id input is required when build_method=existing"
            exit 1
          fi
          echo "BUILD_ID=$BUILD_ID" >> $GITHUB_ENV
          echo "BUILD_METHOD=expo" >> $GITHUB_ENV
          echo "✅ Using existing build: $BUILD_ID"

      - name: Build Android App Bundle (Expo Cloud)
        if: github.event.inputs.build_method == 'expo'
        working-directory: src/mobile
        run: |
          echo "🏗️ Building Android App Bundle (Expo EAS Cloud)..."
          echo "  Profile: ${{ needs.prepare.outputs.build-profile }}"
          echo "  Platform: android"

          # Submit build and capture exact build ID for this run
          set +e
          BUILD_OUTPUT=$(eas build \
            --profile ${{ needs.prepare.outputs.build-profile }} \
            --platform android \
            --json \
            --non-interactive \
            --no-wait 2>&1)
          EAS_EXIT_CODE=$?

          echo "$BUILD_OUTPUT"

          # Extract build ID from EAS output (non-JSON lines may precede the JSON array)
          BUILD_ID=$(echo "$BUILD_OUTPUT" | grep -o '"id": *"[^"]*"' | head -1 | grep -o '"[^"]*"$' | tr -d '"')

          if [ -z "$BUILD_ID" ]; then
            echo "❌ Could not extract build ID from EAS output"
            echo "❌ EAS exit code: $EAS_EXIT_CODE"
            exit 1
          else
            echo "BUILD_ID=$BUILD_ID" >> $GITHUB_ENV
            echo "BUILD_METHOD=expo" >> $GITHUB_ENV
            echo "✅ Build ID: $BUILD_ID (build queued on Expo Cloud, EAS exit=$EAS_EXIT_CODE)"
          fi
          set -e
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      - name: Build Android App Bundle (EAS Local)
        if: github.event.inputs.build_method == 'local-eas'
        working-directory: src/mobile
        run: |
          echo "🏗️ Building Android App Bundle (Local EAS)..."
          echo "  Profile: ${{ needs.prepare.outputs.build-profile }}"
          echo "  Platform: android"
          echo "  Note: Uses local machine instead of Expo cloud (free)"

          set +e
          eas build \
            --profile ${{ needs.prepare.outputs.build-profile }} \
            --platform android \
            --local \
            --non-interactive 2>&1
          BUILD_EXIT_CODE=$?
          set -e

          if [ $BUILD_EXIT_CODE -ne 0 ]; then
            echo "❌ Local build failed with exit code $BUILD_EXIT_CODE"
            echo "📝 Note: Local builds require:"
            echo "   - Android SDK installed (requires ~10GB)"
            echo "   - AVD (Android Virtual Device) configured"
            echo "   - ANDROID_HOME environment variable set"
            echo "   - 30-60 minutes build time"
            echo ""
            echo "💡 For CI/CD, use 'expo' method (cloud builds)"
            exit 1
          fi

          # For local builds, EAS writes a build-*.aab in the project root
          AAB_PATH=$(ls -t *.aab 2>/dev/null | head -1 || true)
          if [ -z "$AAB_PATH" ]; then
            AAB_PATH=$(find android/app/build/outputs/bundle -type f -name "*.aab" 2>/dev/null | sort | tail -1 || true)
          fi

          if [ -z "$AAB_PATH" ] || [ ! -f "$AAB_PATH" ]; then
            echo "❌ AAB not found after local build"
            exit 1
          fi

          AAB_FULL_PATH=$(realpath "$AAB_PATH")
          echo "✅ AAB built successfully at $AAB_FULL_PATH"
          echo "BUILD_METHOD=local-eas" >> $GITHUB_ENV
          echo "AAB_PATH=$AAB_FULL_PATH" >> $GITHUB_ENV
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      - name: Wait for Expo build completion
        if: env.BUILD_METHOD == 'expo' && env.BUILD_ID != ''
        working-directory: src/mobile
        run: |
          echo "⏳ Waiting for Expo build to complete (~5-7 minutes, max 30 min)..."

          MAX_WAIT=1800
          ELAPSED=0
          SLEEP_INTERVAL=30

          while [ $ELAPSED -lt $MAX_WAIT ]; do
            RAW_INFO=$(eas build:view "$BUILD_ID" --json 2>&1 || true)
            # Strip spinner/non-JSON lines before the first { or [
            BUILD_INFO=$(echo "$RAW_INFO" | awk '/^[{\[]/{found=1} found{print}')

            if [ -n "$BUILD_INFO" ] && echo "$BUILD_INFO" | jq empty 2>/dev/null; then
              BUILD_STATUS=$(echo "$BUILD_INFO" | jq -r 'if type=="array" then (.[0].status // "unknown") else (.status // "unknown") end' | tr '[:upper:]' '[:lower:]')
              BUILD_STATUS=${BUILD_STATUS:-unknown}

              echo "⏳ Build $BUILD_ID status: $BUILD_STATUS (${ELAPSED}s elapsed)"

              if [ "$BUILD_STATUS" == "finished" ]; then
                echo "✅ Build completed successfully!"
                break
              elif [ "$BUILD_STATUS" == "errored" ]; then
                echo "❌ Build failed!"
                exit 1
              elif [ "$BUILD_STATUS" == "canceled" ]; then
                echo "❌ Build was canceled!"
                exit 1
              fi
            else
              echo "⏳ Waiting for build status to be available... (${ELAPSED}s elapsed)"
            fi

            sleep $SLEEP_INTERVAL
            ELAPSED=$((ELAPSED + SLEEP_INTERVAL))
          done

          if [ $ELAPSED -ge $MAX_WAIT ]; then
            echo "❌ Build timed out after ${MAX_WAIT} seconds"
            exit 1
          fi
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      - name: Download AAB from Expo
        if: env.BUILD_METHOD == 'expo' && env.BUILD_ID != ''
        working-directory: src/mobile
        run: |
          echo "📥 Downloading AAB from Expo..."

          RAW_INFO=$(eas build:view "$BUILD_ID" --json 2>&1)
          BUILD_INFO=$(echo "$RAW_INFO" | awk '/^[{\[]/{found=1} found{print}')
          AAB_URL=$(echo "$BUILD_INFO" | jq -r '.artifacts.buildUrl // empty')

          if [ -z "$AAB_URL" ]; then
            echo "❌ Could not extract AAB URL from build"
            echo "Build info: $BUILD_INFO"
            exit 1
          fi

          echo "Downloading from: $AAB_URL"
          curl -L -o app-release.aab "$AAB_URL"

          if [ ! -f app-release.aab ]; then
            echo "❌ Failed to download AAB"
            exit 1
          fi

          FILE_SIZE=$(du -h app-release.aab | cut -f1)
          echo "✅ AAB downloaded successfully ($FILE_SIZE)"
          echo "AAB_PATH=$(pwd)/app-release.aab" >> $GITHUB_ENV
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      - name: Firebase Test Lab (Optional)
        continue-on-error: true
        run: |
          echo "📋 Firebase Test Lab Testing"
          echo "  Status: Skipped (requires GCP_SA_KEY and GCP_PROJECT_ID secrets)"
          echo "  To enable: Set GCP_SA_KEY and GCP_PROJECT_ID repository secrets"
          echo "  Test AAB at: assets/app-release.aab"

      - name: Submit to Google Play Store (Expo)
        if: env.BUILD_METHOD == 'expo'
        working-directory: src/mobile
        run: |
          echo "📤 Submitting to Google Play Store (Expo)..."
          echo "  Track: ${{ needs.prepare.outputs.track }}"
          echo "  Build ID: ${BUILD_ID}"

          if [ -z "${BUILD_ID:-}" ]; then
            echo "❌ BUILD_ID is missing; refusing to submit"
            exit 1
          fi

          # Submit exact build from this workflow run via EAS
          eas submit \
            --platform android \
            --profile ${{ needs.prepare.outputs.environment }} \
            --id "$BUILD_ID" \
            --non-interactive
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      - name: Submit to Google Play Store (Local EAS)
        if: env.BUILD_METHOD == 'local-eas'
        working-directory: src/mobile
        run: |
          echo "📤 Submitting to Google Play Store (EAS Submit)..."
          echo "  Track: ${{ needs.prepare.outputs.track }}"
          echo "  AAB: ${AAB_PATH}"

          if [ -z "${AAB_PATH:-}" ] || [ ! -f "${AAB_PATH}" ]; then
            echo "❌ AAB file not found at ${AAB_PATH}"
            exit 1
          fi

          # Use EAS submit (preferred over Play Console API for simplicity)
          eas submit \
            --platform android \
            --profile ${{ needs.prepare.outputs.environment }} \
            --path "${AAB_PATH}" \
            --non-interactive
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      - name: Cleanup secrets
        if: always()
        run: |
          rm -f src/mobile/secrets/google-play-service-account.json
          echo "🧹 Cleaned up secrets"

      - name: Create release notes
        id: release-notes
        run: |
          NOTES="${{ github.event.inputs.release_notes || 'Bug fixes and performance improvements' }}"
          ENV="${{ needs.prepare.outputs.environment }}"
          VERSION="${{ needs.prepare.outputs.version }}"
          TRACK="${{ needs.prepare.outputs.track }}"

          cat > release_notes.md << EOF
          # WAOOAW Mobile - $ENV Release

          **Version**: $VERSION
          **Track**: $TRACK
          **Build Date**: $(date -u +"%Y-%m-%d %H:%M:%S UTC")

          ## Release Notes

          $NOTES

          ## Configuration
          - **Environment**: $ENV
          - **Backend**: ${{ needs.prepare.outputs.environment == 'demo' && 'https://waooaw-api-demo-ryvhxvrdna-el.a.run.app' || 'https://api.waooaw.com' }}
          - **Build Profile**: ${{ needs.prepare.outputs.build-profile }}
          - **Distribution**: Google Play Store ($TRACK track)

          ## Links
          - [Play Console](https://play.google.com/console)
          - [EAS Builds](https://expo.dev/accounts/waooaw/projects/waooaw-mobile/builds)

          ## Next Steps
          1. Monitor crash reports in Play Console
          2. Check user reviews
          3. Monitor backend metrics
          4. Plan next release
          EOF

          cat release_notes.md

      - name: Create GitHub Release
        if: needs.prepare.outputs.is-production == 'true'
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref_name }}
          release_name: Mobile v${{ needs.prepare.outputs.version }} - Play Store
          body_path: release_notes.md
          draft: false
          prerelease: false

      - name: Upload build artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playstore-deployment-${{ needs.prepare.outputs.version }}
          path: |
            release_notes.md
            src/mobile/app.json
            src/mobile/*.aab
            src/mobile/build-*.aab
          retention-days: 90

  notify:
    name: Send Notifications
    runs-on: ubuntu-latest
    needs: [prepare, build-and-submit]
    if: always()
    env:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL || secrets.SLACK_WEBHOOK }}

    steps:
      - name: Notify Slack
        if: always() && env.SLACK_WEBHOOK_URL != ''
        uses: 8398a7/action-slack@v3
        with:
          status: ${{ job.status }}
          text: |
            🚀 *Google Play Store Deployment*

            *Status*: ${{ job.status == 'success' && '✅ Success' || '❌ Failed' }}
            *Environment*: ${{ needs.prepare.outputs.environment }}
            *Version*: ${{ needs.prepare.outputs.version }}
            *Track*: ${{ needs.prepare.outputs.track }}

            *Links*:
            • <https://play.google.com/console|Play Console>
            • <https://expo.dev/accounts/waooaw/projects/waooaw-mobile/builds|EAS Builds>
            • <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|GitHub Actions>


      - name: Comment on PR (if applicable)
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const status = '${{ job.status }}' === 'success' ? '✅' : '❌';
            const body = `${status} **Google Play Store Deployment**

            - **Environment**: ${{ needs.prepare.outputs.environment }}
            - **Version**: ${{ needs.prepare.outputs.version }}
            - **Track**: ${{ needs.prepare.outputs.track }}
            - **Status**: ${{ job.status }}

            [View in Play Console](https://play.google.com/console) | [View Build](https://expo.dev/accounts/waooaw/projects/waooaw-mobile/builds)`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });

  rollback-plan:
    name: Prepare Rollback Plan
    runs-on: ubuntu-latest
    needs: [prepare, build-and-submit]
    if: failure() && needs.prepare.outputs.is-production == 'true'
    env:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL || secrets.SLACK_WEBHOOK }}

    steps:
      - name: Create rollback instructions
        run: |
          cat > rollback_plan.md << 'EOF'
          # 🚨 Rollback Plan - Production Deployment Failed

          ## Immediate Actions

          1. **Halt rollout in Play Console**:
             ```
             - Go to: https://play.google.com/console
             - Navigate to: Release > Production
             - Click "Halt rollout" on the latest release
             ```

          2. **Revert to previous version**:
             ```bash
             cd /workspaces/WAOOAW/src/mobile

             # Find previous successful build
             eas build:list --platform android --status finished --limit 5

             # Submit previous build
             eas submit --platform android --id <PREVIOUS_BUILD_ID>
             ```

          3. **Notify users** (if already rolled out):
             - Post in app notification
             - Email to affected users
             - Status page update

          ## Investigation

          1. Check EAS build logs: https://expo.dev/accounts/waooaw/projects/waooaw-mobile/builds
          2. Check Play Console crash reports
          3. Review backend logs for API errors
          4. Check GitHub Actions logs

          ## Prevention

          - Run full test suite before production deployment
          - Use internal track for initial testing
          - Gradual rollout (10% → 50% → 100%)
          - Monitor crash-free rate in first 24 hours

          EOF

          cat rollback_plan.md

      - name: Upload rollback plan
        uses: actions/upload-artifact@v4
        with:
          name: rollback-plan-${{ github.run_id }}
          path: rollback_plan.md
          retention-days: 30

      - name: Alert team
        if: env.SLACK_WEBHOOK_URL != ''
        uses: 8398a7/action-slack@v3
        with:
          status: custom
          custom_payload: |
            {
              text: "🚨 *PRODUCTION DEPLOYMENT FAILED* 🚨",
              attachments: [{
                color: 'danger',
                text: "Mobile app deployment to production Play Store failed. Rollback plan generated.",
                fields: [
                  { title: "Version", value: "${{ needs.prepare.outputs.version }}", short: true },
                  { title: "Track", value: "${{ needs.prepare.outputs.track }}", short: true }
                ],
                actions: [{
                  type: "button",
                  text: "View Logs",
                  url: "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
                }]
              }]
            }
