# Google OAuth Configuration
# Get these from https://console.cloud.google.com/apis/credentials

# Google OAuth Client ID (from Google Cloud Console)
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Note: The above variable is used in the frontend.
# Format: VITE_ prefix exposes it to Vite build system.
#
# To get your Google Client ID:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable Google+ API or Google Identity Services
# 4. Go to Credentials
# 5. Create OAuth 2.0 Client ID
# 6. Add authorized JavaScript origins:
#    - http://localhost:8080 (development)
#    - https://*.app.github.dev (Codespaces)
#    - https://www.waooaw.com (production)
# 7. Add authorized redirect URIs (not needed for modal OAuth, but good to have):
#    - http://localhost:8080/auth/callback
#    - https://*.app.github.dev/auth/callback
#    - https://www.waooaw.com/auth/callback
# 8. Copy the Client ID here

# API Backend URL (automatically detected, but can override)
# VITE_API_URL=http://localhost:8000
