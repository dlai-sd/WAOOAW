/**
 * OAuth Components Barrel Export
 * Makes imports cleaner across portals
 * 
 * Usage:
 *   import { GoogleOAuth, OAuthCallback } from './components/OAuth';
 */

export { default as GoogleOAuth } from './GoogleOAuth';
export { default as OAuthCallback } from './OAuthCallback';
