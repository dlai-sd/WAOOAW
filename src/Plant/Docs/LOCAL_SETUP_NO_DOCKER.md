# Local Setup (No Docker)

## Deprecated: Docker-only repository

WAOOAW is a Docker-only repo.

- Use Docker Compose for local development and testing.
- If a specific no-Docker workflow is required, request it explicitly and we can design a supported path.

