# WAOOAW Test Dependencies
# Consolidated requirements for all test types

# Core Testing Framework
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
pytest-mock==3.12.0
pytest-benchmark==4.0.0
pytest-xdist==3.5.0  # Parallel test execution
pytest-timeout==2.2.0

# Test Containers (for integration tests with real services)
testcontainers==3.7.1

# Database + ORM (needed for integration tests)
sqlalchemy==2.0.25
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.1
pgvector==0.2.4

# Web Framework (for API testing)
fastapi==0.109.0
httpx==0.26.0
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0.post1  # Required by pydantic for EmailStr validation

# Auth + Security (for auth testing)
PyJWT==2.8.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==42.0.2

# OAuth
authlib==1.3.0

# Cache (for caching tests)
redis==5.0.1

# Utils
python-dotenv==1.0.0
pyyaml==6.0.1
Faker==40.1.2  # Test data generation
flake8==7.0.0
tenacity==8.2.3

# Metrics (required by CP backend imports during test collection)
prometheus-client==0.19.0

# Coverage Reporting
coverage[toml]==7.4.0
